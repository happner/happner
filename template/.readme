can be used as follows in the repo clone

bin/happner --conf template/config/server.js

to be used as follows 

happner --create [name]

   -> to copy all this, as template, to cwd/name/...
      or something like that.

installs a www comman into the prompt, to call up the browser to the www module 

