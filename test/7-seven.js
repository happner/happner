describe('between 6 and 8', function() {

  var to = and = it;
  
  to('bring completeness', function(done) {
    done();
  });

  and('bring to full fruition', function(done) {
    done();
  });

  xit('snaps shut "The Loop"', function(done) {
    done( to(Infinity) && beyond );
  })

});
